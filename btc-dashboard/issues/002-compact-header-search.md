Title: Add compact header with search and small typography

Description
-----------
Improve header with a compact layout inspired by Google Finance: smaller typography, a search box for blocks/txs, and a clean action area for theme and status. Keep accessibility and keyboard navigation in mind.

Acceptance criteria
- Add a compact header in `templates/index.html` with a search input that can query the existing HTTP API (or placeholder if API not ready).
- Provide keyboard focus styles and aria labels.
- Add small typography variants in CSS (via Tailwind classes or CSS variables).

Estimate: 1-2 hours
