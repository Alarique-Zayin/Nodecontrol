Title: Add responsive small/large-card layout and theme CSS variables

Description
-----------
Implement a responsive layout where metric cards can expand into larger detail cards. Add CSS variables for theme colors to enable consistent dark/light theming.

Acceptance criteria
- Define CSS variables for primary/secondary/background/foreground colors in `templates/index.html` or a small CSS file.
- Implement responsive card classes that switch grid layout and card size based on breakpoints.
- Provide a demo where clicking a metric card expands it into a larger panel showing a larger chart and recent values.

Estimate: 2-3 hours
